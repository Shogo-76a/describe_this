<!-- ヘッダーの内容変更 -->
<% provide(:page_name) do %>
    <div class="flex items-end gap-0">
        <p class="text-2xl font-normal opacity-75"><%= t(".pass_reset") %></p>
    </div>
<% end %>


<div class="min-h-[calc(100vh-15rem)] flex flex-col justify-center items-center max-w-sm mx-auto mt-10">
  <% if alert = flash[:alert] %>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block" id="alert"><%= alert %></p>
  <% end %>

  <%= form_with url: passwords_path, class: "flex flex-col w-full items-center justify-center gap-3" do |form| %>
    <div class="w-full">
      <%= form.label :email_address, t(".email") %>
      <%= form.email_field :email_address, required: true, autocomplete: "username", value: params[:email_address], 
      class: "mt-1 items-center justify-center input input-bordered rounded-full w-full"%>
    </div>

    <%= form.submit t(".submit"), class: "mt-3 btn btn-primary btn-text-primary w-full rounded-full" %>
  <% end %>
</div>
