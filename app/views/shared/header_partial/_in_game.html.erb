<!-- 両端にホーム用ボタン(レコード削除機能) と テーマボタン があるヘッダー -->
<header class="flex justify-between items-center navbar bg-base-200 text-base-content p-2">
    <% if authenticated? %>
        <%= button_to user_game_path(@game.user_id, @game), method: :delete, class: "inline-block" do %>
            <div 
                class="bg-base-content/75 w-11 h-11" 
                style="
                -webkit-mask-image: url('<%= asset_path("DT_logo.png") %>');
                mask-image: url('<%= asset_path("DT_logo.png") %>');
                -webkit-mask-size: contain;
                mask-size: contain;
                -webkit-mask-repeat: no-repeat;
                mask-repeat: no-repeat;
                -webkit-mask-position: center;
                mask-position: center;
                "
            >
            </div>
        <% end %>
    <% elsif %>
        <%= button_to guest_game_path(@game), method: :delete, class: "inline-block" do %>
            <div 
                class="bg-base-content/75 w-11 h-11" 
                style="
                -webkit-mask-image: url('<%= asset_path("DT_logo.png") %>');
                mask-image: url('<%= asset_path("DT_logo.png") %>');
                -webkit-mask-size: contain;
                mask-size: contain;
                -webkit-mask-repeat: no-repeat;
                mask-repeat: no-repeat;
                -webkit-mask-position: center;
                mask-position: center;
                "
            >
            </div>
        <% end %>
    <% end %>
    <%= yield :in_game %>
    <%= render 'shared/theme_color_tab', theme_name: 'light' %>
</header>