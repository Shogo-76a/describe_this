<!-- ヘッダーの内容変更 -->
<% provide(:header_content) do %>
    <div class="flex items-end gap-0">
        <p class="text-2xl font-bold">採点結果</p>
    </div>
<% end %>

<!-- 採点Job からの通信(WebSocket)をみる。採点でき次第差し替える -->
<%= turbo_stream_from "resulting_score_#{@game.id}" %>

<div id="resulting_score">
    <div class="hero min-h-screen">
      <div class="hero-content text-center">
        <div class="grid place-items-center text-center w-full">
          <p class="text-2xl">
            採点中
          </p>
          <div class="text-xm my-5">
            <span class="loading loading-spinner loading-md"></span>
          </div>
        </div>
      </div>
    </div>
</div>

<!-- フッターの内容変更 -->
<% provide(:footer_content) do %>
    <!-- 空欄 -->
<% end %>